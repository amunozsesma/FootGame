"use strict";

var config = {
	numPlayers: 3,
    numRows: 5,
    numColumns: 11,
    overallTimeout: 30000, 
    compareStrategy: "same-cell",
    resolveStrategy: "defender-wins"
};

module.exports = config;