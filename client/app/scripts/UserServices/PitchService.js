PitchService(pitchContainerElement)

drawPlayers([positionTeam1Player1, positionTeam1Player2], [positionTeam2Player1, positionTeam2Player2]);
drawBall(position)
movePlayer(initialPosition, endPosition)
passBall(initialPosition, endPosition)
shoot(initialPosition, endPosition, isGoal, isRebound)

addListener({
	playerClicked(position)
})